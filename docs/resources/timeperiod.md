---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "nagios_timeperiod Resource - terraform-provider-nagios"
subcategory: ""
description: |-
  Manages a Nagios XI timeperiod.
---

# nagios_timeperiod (Resource)

Manages a Nagios XI timeperiod.

## Example Usage

```terraform
# Every attribute requires replacing the resource to change - Nagios's PUT
# for timeperiod is a confirmed no-op (see internal/client/timeperiod.go),
# so an in-place update can never actually take effect.
resource "nagios_timeperiod" "business_hours" {
  name      = "business_hours"
  alias     = "Business Hours"
  monday    = "09:00-17:00"
  tuesday   = "09:00-17:00"
  wednesday = "09:00-17:00"
  thursday  = "09:00-17:00"
  friday    = "09:00-17:00"
  exclude   = ["us-holidays"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `alias` (String) The description of the timeperiod.
- `name` (String) The name of the timeperiod.

### Optional

- `exclude` (Set of String) Names of other timeperiods whose time ranges should be excluded from this timeperiod.
- `friday` (String) Time range(s) for Friday, e.g. "09:00-17:00" or "09:00-12:00,13:00-17:00". Omit to leave Friday unavailable.
- `monday` (String) Time range(s) for Monday, e.g. "09:00-17:00" or "09:00-12:00,13:00-17:00". Omit to leave Monday unavailable.
- `saturday` (String) Time range(s) for Saturday, e.g. "09:00-17:00" or "09:00-12:00,13:00-17:00". Omit to leave Saturday unavailable.
- `sunday` (String) Time range(s) for Sunday, e.g. "09:00-17:00" or "09:00-12:00,13:00-17:00". Omit to leave Sunday unavailable.
- `templates` (Set of String) Names of other timeperiod templates this timeperiod should inherit from. Maps to Nagios's `use` field - named `templates` here to match the same field on nagios_host/nagios_service/nagios_contact.
- `thursday` (String) Time range(s) for Thursday, e.g. "09:00-17:00" or "09:00-12:00,13:00-17:00". Omit to leave Thursday unavailable.
- `tuesday` (String) Time range(s) for Tuesday, e.g. "09:00-17:00" or "09:00-12:00,13:00-17:00". Omit to leave Tuesday unavailable.
- `wednesday` (String) Time range(s) for Wednesday, e.g. "09:00-17:00" or "09:00-12:00,13:00-17:00". Omit to leave Wednesday unavailable.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Timeperiods can be imported by their name.
terraform import nagios_timeperiod.business_hours business_hours
```
